# Example backend env (do not commit secrets)
# Copy to .env and fill real values locally

PORT=5000
MONGO_URI=mongodb://localhost:27017/mydb
JWT_SECRET=your_jwt_secret_here

# Any other env variables used by backend
